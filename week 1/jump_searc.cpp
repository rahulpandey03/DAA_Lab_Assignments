#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int t, i, j, n;
    cin >> t;
    while (t)
    {
        cin >> n;
        int a[n];
        for (i = 0; i < n; i++)
            cin >> a[i];
        t--;
    }
    return 0;
}